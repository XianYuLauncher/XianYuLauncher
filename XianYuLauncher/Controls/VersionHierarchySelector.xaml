<UserControl
    x:Class="XianYuLauncher.Controls.VersionHierarchySelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:XianYuLauncher.Helpers"
    xmlns:viewModels="using:XianYuLauncher.ViewModels"
    mc:Ignorable="d"
    x:Name="Root">

    <UserControl.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <helpers:VersionCountToStringConverter x:Key="VersionCountToStringConverter" />
        <helpers:FirstLetterToUpperConverter x:Key="FirstLetterToUpperConverter" />
        
        <!-- 覆盖VersionManagementPage的深色卡片样式，使用系统默认的浅色 -->
        <SolidColorBrush x:Key="CardBackgroundFillColorDefaultBrush" Color="{ThemeResource LayerFillColorDefault}" />
        <SolidColorBrush x:Key="CardStrokeColorDefaultBrush" Color="{ThemeResource CardStrokeColorDefault}" />
    </UserControl.Resources>

    <ListView ItemsSource="{x:Bind ItemsSource, Mode=OneWay}"
              SelectionMode="None"
              IsItemClickEnabled="False"
              HorizontalAlignment="Stretch"
              HorizontalContentAlignment="Stretch">
        <ListView.ItemContainerStyle>
            <Style TargetType="ListViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Margin" Value="0,0,0,4" />
            </Style>
        </ListView.ItemContainerStyle>
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="viewModels:ModpackGameVersionViewModel">
                <Expander HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Stretch"
                          IsExpanded="{x:Bind IsExpanded, Mode=TwoWay}"
                          Padding="0">
                    <Expander.Header>
                        <Grid Padding="4,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{x:Bind GameVersion}"
                                       FontSize="14"
                                       FontWeight="Medium"
                                       VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1"
                                       Text="{x:Bind TotalVersionCount, Mode=OneWay, Converter={StaticResource VersionCountToStringConverter}}"
                                       FontSize="13"
                                       Foreground="{ThemeResource TextFillColorSecondary}"
                                       VerticalAlignment="Center"
                                       Margin="8,0,0,0" />
                        </Grid>
                    </Expander.Header>
                    <Expander.Content>
                        <StackPanel Spacing="8" Margin="0,8,0,0">
                            <ItemsControl ItemsSource="{x:Bind Loaders}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="viewModels:ModpackLoaderViewModel">
                                        <Expander HorizontalAlignment="Stretch"
                                                  HorizontalContentAlignment="Stretch"
                                                  IsExpanded="{x:Bind IsExpanded, Mode=TwoWay}"
                                                  Margin="0,0,0,4"
                                                  Padding="0">
                                            <Expander.Header>
                                                <Grid Padding="4,6">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <FontIcon Grid.Column="0"
                                                              Glyph="&#xE74C;"
                                                              FontSize="14"
                                                              Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                                                              VerticalAlignment="Center"
                                                              Margin="0,0,8,0" />
                                                    <TextBlock Grid.Column="1"
                                                               Text="{x:Bind LoaderName}"
                                                               FontSize="14"
                                                               FontWeight="Medium"
                                                               VerticalAlignment="Center" />
                                                    <TextBlock Grid.Column="2"
                                                               Text="{x:Bind Versions.Count, Mode=OneWay, Converter={StaticResource VersionCountToStringConverter}}"
                                                               FontSize="13"
                                                               Foreground="{ThemeResource TextFillColorSecondary}"
                                                               VerticalAlignment="Center"
                                                               Margin="8,0,0,0" />
                                                </Grid>
                                            </Expander.Header>
                                            <Expander.Content>
                                                <ListView ItemsSource="{x:Bind Versions}"
                                                          SelectionMode="None"
                                                          IsItemClickEnabled="False"
                                                          Margin="0,4,0,0">
                                                    <ListView.ItemContainerStyle>
                                                        <Style TargetType="ListViewItem">
                                                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                            <Setter Property="Padding" Value="0" />
                                                            <Setter Property="Margin" Value="0,0,0,8" />
                                                        </Style>
                                                    </ListView.ItemContainerStyle>
                                                    <ListView.ItemTemplate>
                                                        <DataTemplate x:DataType="viewModels:ModpackVersionViewModel">
                                                            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                                    BorderThickness="1"
                                                                    CornerRadius="6"
                                                                    Padding="12">
                                                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*" />
                                                                        <ColumnDefinition Width="56" />
                                                                    </Grid.ColumnDefinitions>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                    </Grid.RowDefinitions>

                                                                    <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Spacing="8">
                                                                        <TextBlock Text="{x:Bind VersionNumberText}"
                                                                                   FontSize="15"
                                                                                   FontWeight="SemiBold"
                                                                                   VerticalAlignment="Center" />
                                                                        <Border CornerRadius="4"
                                                                                Padding="6,2"
                                                                                Background="{ThemeResource SubtleFillColorSecondaryBrush}">
                                                                            <TextBlock Text="{x:Bind VersionTypeText, Converter={StaticResource FirstLetterToUpperConverter}}"
                                                                                       FontSize="11"
                                                                                       Foreground="{ThemeResource TextFillColorSecondary}" />
                                                                        </Border>
                                                                    </StackPanel>

                                                                    <TextBlock Grid.Row="1"
                                                                               Grid.Column="0"
                                                                               FontSize="12"
                                                                               Foreground="{ThemeResource TextFillColorSecondary}"
                                                                               Margin="0,4,0,0">
                                                                        <Run x:Uid="ModDownloadDetailPage_ReleaseDateText" Text="发布日期: " />
                                                                        <Run Text="{x:Bind ReleaseDateText}" />
                                                                    </TextBlock>

                                                                    <TextBlock Grid.Row="2"
                                                                               Grid.Column="0"
                                                                               Text="{x:Bind FileNameText}"
                                                                               FontSize="11"
                                                                               Foreground="{ThemeResource TextFillColorTertiary}"
                                                                               TextTrimming="CharacterEllipsis"
                                                                               Margin="0,2,12,0"
                                                                               Visibility="{x:Bind ShowFileName, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

                                                                    <RadioButton Grid.Row="0"
                                                                                 Grid.RowSpan="3"
                                                                                 Grid.Column="1"
                                                                                 HorizontalAlignment="Right"
                                                                                 VerticalAlignment="Center"
                                                                                 AutomationProperties.Name="{x:Bind VersionNumberText}"
                                                                                 MinWidth="28"
                                                                                 MinHeight="28"
                                                                                 Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                                                                 Padding="0"
                                                                                 Margin="0,0,12,0"
                                                                                 GroupName="{x:Bind SelectionGroupName}"
                                                                                 IsChecked="{x:Bind IsSelected, Mode=TwoWay}"
                                                                                 Visibility="Visible"
                                                                                 Checked="SelectRadio_Checked"
                                                                                 Tag="{x:Bind}" />
                                                                </Grid>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ListView.ItemTemplate>
                                                </ListView>
                                            </Expander.Content>
                                        </Expander>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Spacing="0" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </StackPanel>
                    </Expander.Content>
                </Expander>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>