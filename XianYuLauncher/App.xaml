<Application
    x:Class="XianYuLauncher.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <ResourceDictionary Source="/Styles/FontSizes.xaml" />
                <ResourceDictionary Source="/Styles/Thickness.xaml" />
                <ResourceDictionary Source="/Styles/TextBlock.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" xmlns:helpers="using:XianYuLauncher.Helpers" />
            <helpers:BoolNegationConverter x:Key="BoolNegationConverter" xmlns:helpers="using:XianYuLauncher.Helpers" />
            <helpers:BoolToBrushConverter x:Key="BoolToBrushConverter" xmlns:helpers="using:XianYuLauncher.Helpers" />
            <helpers:BoolToFontWeightConverter x:Key="BoolToFontWeightConverter" xmlns:helpers="using:XianYuLauncher.Helpers" />
            <helpers:BoolToTextConverter x:Key="BoolToTextConverter" xmlns:helpers="using:XianYuLauncher.Helpers" />
            <helpers:ProfileTypeConverter x:Key="ProfileTypeConverter" xmlns:helpers="using:XianYuLauncher.Helpers" />
            <helpers:DateTimeConverter x:Key="DateTimeConverter" xmlns:helpers="using:XianYuLauncher.Helpers" />
            <helpers:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" xmlns:helpers="using:XianYuLauncher.Helpers" />
            <helpers:StringNullOrEmptyToBoolConverter x:Key="StringNullOrEmptyToBoolConverter" xmlns:helpers="using:XianYuLauncher.Helpers" />
            <helpers:ProgressVisibilityConverter x:Key="ProgressVisibilityConverter" xmlns:helpers="using:XianYuLauncher.Helpers" />
            <helpers:UriToImageSourceConverter x:Key="UriToImageSourceConverter" xmlns:helpers="using:XianYuLauncher.Helpers" />
            <helpers:BoolToVisibilityInverseConverter x:Key="BoolToVisibilityInverseConverter" xmlns:helpers="using:XianYuLauncher.Helpers" />
            <helpers:StringNullOrEmptyToVisibilityConverter x:Key="StringNullOrEmptyToVisibilityConverter" xmlns:helpers="using:XianYuLauncher.Helpers" />
            <helpers:DateTimeToStringConverter x:Key="DateTimeToStringConverter" xmlns:helpers="using:XianYuLauncher.Helpers" />
            
            <!-- 轮询内容模板，用于显示轮询结果 -->
            <DataTemplate x:Key="PollingContentTemplate">
                <StackPanel Spacing="16">
                    <TextBlock 
                        x:Uid="联机Page_PollingContent"
                        Text="请启动Minecraft,选择 对局域网开放 选项，然后运行联机服务即可" 
                        Style="{ThemeResource BodyTextBlockStyle}" />
                    <StackPanel Spacing="8">
                        <TextBlock 
                            x:Uid="联机Page_PollingResultLabel"
                            Text="轮询结果:" 
                            Style="{ThemeResource CaptionTextBlockStyle}" 
                            FontWeight="SemiBold" />
                        <Border 
                            Background="{ThemeResource CardBackgroundFillColorDefault}"
                            BorderBrush="{ThemeResource CardStrokeColorDefault}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16">
                            <TextBlock 
                                Text="{Binding PollingResult, Mode=OneWay}" 
                                Style="{ThemeResource BodyTextBlockStyle}" 
                                TextWrapping="WrapWholeWords" 
                                FontFamily="Consolas" 
                                FontSize="12" />
                        </Border>
                    </StackPanel>
                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
