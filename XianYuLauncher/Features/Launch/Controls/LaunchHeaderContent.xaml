<UserControl
    x:Class="XianYuLauncher.Features.Launch.Controls.LaunchHeaderContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:animations="using:CommunityToolkit.WinUI.Animations"
    mc:Ignorable="d">

    <StackPanel Width="420" HorizontalAlignment="Left" Spacing="4">
        <animations:Implicit.Animations>
            <animations:OffsetAnimation Duration="0:0:0.3" EasingMode="EaseOut" EasingType="Default" />
        </animations:Implicit.Animations>

        <TextBlock
            Text="{x:Bind ViewModel.PageTitle, Mode=OneWay}"
            FontSize="{x:Bind ViewModel.PageTitleFontSize, Mode=OneWay}"
            Style="{StaticResource TitleTextBlockStyle}"
            FontWeight="SemiBold"
            TextWrapping="WrapWholeWords" />

        <TextBlock
            Text="XianYu Launcher"
            Style="{StaticResource CaptionTextBlockStyle}"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
    </StackPanel>
</UserControl>