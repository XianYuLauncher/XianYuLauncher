<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="XianYuLauncher.Features.WorldManagement.Views.WorldManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:XianYuLauncher.Helpers"
    xmlns:models="using:XianYuLauncher.Models.VersionManagement"
    xmlns:animations="using:CommunityToolkit.WinUI.Animations"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Page.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <helpers:StringNullOrEmptyToBoolConverter x:Key="StringNullOrEmptyToVisibilityConverter"/>
    </Page.Resources>

    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">
        <Grid Padding="24,16,24,24"
              HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100*" MaxWidth="1064"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1"
                        Spacing="24"
                        HorizontalAlignment="Stretch">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Command="{x:Bind ViewModel.GoBackCommand}"
                            Style="{StaticResource SubtleButtonStyle}"
                            Margin="0,0,16,0">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE72B;" FontSize="16"/>
                            <TextBlock Text="返回" x:Uid="WorldManagement_Back" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <TextBlock Grid.Column="1"
                               Text="{x:Bind ViewModel.WorldName, Mode=OneWay}"
                               Style="{StaticResource TitleTextBlockStyle}"
                               VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                  BorderThickness="1"
                  CornerRadius="8"
                  Padding="32">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Width="128"
                        Height="128"
                        CornerRadius="8"
                        Background="{ThemeResource LayerFillColorDefaultBrush}"
                        Margin="0,0,32,0">
                    <Grid>
                        <FontIcon Glyph="&#xE7C3;"
                                  FontSize="44"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                        <Image Source="{x:Bind ViewModel.WorldIcon, Mode=OneWay, FallbackValue={x:Null}}"
                               Stretch="UniformToFill"/>
                    </Grid>
                </Border>

                <StackPanel Grid.Column="1" Spacing="12" VerticalAlignment="Center">
                    <TextBlock Text="{x:Bind ViewModel.WorldName, Mode=OneWay}"
                               Style="{StaticResource SubtitleTextBlockStyle}"
                               FontWeight="SemiBold"/>

                    <StackPanel Orientation="Horizontal" Spacing="24">
                        <StackPanel Spacing="4">
                            <TextBlock Text="游戏模式"
                                       x:Uid="WorldManagement_GameMode"
                                       FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{x:Bind ViewModel.GameMode, Mode=OneWay}"
                                       FontSize="16"
                                       FontWeight="SemiBold"/>
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Text="难度"
                                       x:Uid="WorldManagement_Difficulty"
                                       FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{x:Bind ViewModel.Difficulty, Mode=OneWay}"
                                       FontSize="16"
                                       FontWeight="SemiBold"/>
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Text="世界大小"
                                       x:Uid="WorldManagement_WorldSize"
                                       FontSize="12"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{x:Bind ViewModel.WorldSize, Mode=OneWay}"
                                       FontSize="16"
                                       FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,8,0,0">
                        <Button Style="{StaticResource AccentButtonStyle}">
                            <Button.Flyout>
                                <Flyout Opening="LaunchWorldProfileFlyout_Opening" />
                            </Button.Flyout>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE768;" FontSize="16"/>
                                <TextBlock Text="启动游戏"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{x:Bind ViewModel.OpenWorldFolderCommand}"
                                Style="{StaticResource DefaultButtonStyle}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8B7;" FontSize="16"/>
                                <TextBlock Text="打开文件夹" x:Uid="WorldManagement_OpenFolder"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <StackPanel Spacing="20">
                <SelectorBar x:Name="WorldTabSelectorBar"
                             SelectionChanged="WorldTabSelectorBar_SelectionChanged">
                    <SelectorBarItem x:Name="OverviewTabItem"
                                     Text="概览"
                                     IsSelected="True">
                        <SelectorBarItem.Icon>
                            <FontIcon Glyph="&#xE80F;"/>
                        </SelectorBarItem.Icon>
                    </SelectorBarItem>
                    <SelectorBarItem x:Name="DataPacksTabItem"
                                     Text="数据包">
                        <SelectorBarItem.Icon>
                            <FontIcon Glyph="&#xE8B7;"/>
                        </SelectorBarItem.Icon>
                    </SelectorBarItem>
                </SelectorBar>

                <Grid Visibility="{x:Bind ViewModel.IsOverviewTabSelected, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <animations:Implicit.ShowAnimations>
                        <animations:TranslationAnimation Duration="0:0:0.5" From="0, 40, 0" To="0" EasingMode="EaseOut" />
                        <animations:OpacityAnimation Duration="0:0:0.5" From="0" To="1.0" />
                    </animations:Implicit.ShowAnimations>
                    <Grid ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="20"
                                Spacing="16">
                        <TextBlock Text="基本信息"
                                   x:Uid="WorldOverview_BasicInfo"
                                   Style="{StaticResource BodyStrongTextBlockStyle}"/>

                        <StackPanel Spacing="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="种子"
                                           x:Uid="WorldOverview_Seed"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           VerticalAlignment="Center"/>

                                <Button Grid.Column="1"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Padding="0"
                                        Margin="-4,0,0,0"
                                        HorizontalAlignment="Left"
                                        HorizontalContentAlignment="Left"
                                        VerticalAlignment="Center"
                                        CornerRadius="4"
                                        Click="CopySeedButton_Click"
                                        ToolTipService.ToolTip="点击复制种子"
                                        x:Uid="WorldOverview_CopySeedTooltip">
                                    <Button.Resources>
                                        <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="{ThemeResource SubtleFillColorSecondary}"/>
                                        <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="{ThemeResource SubtleFillColorTertiary}"/>
                                    </Button.Resources>
                                    <Grid Padding="4,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                                   Text="{x:Bind ViewModel.Seed, Mode=OneWay}"
                                                   TextWrapping="Wrap"
                                                   VerticalAlignment="Center"/>
                                        <FontIcon Grid.Column="1"
                                                  Glyph="&#xE8C8;"
                                                  FontSize="14"
                                                  Margin="8,0,0,0"
                                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                  VerticalAlignment="Center"/>
                                    </Grid>
                                </Button>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="创建时间" x:Uid="WorldOverview_CreationTime" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.FormattedCreationTime, Mode=OneWay}"/>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="最后游玩" x:Uid="WorldOverview_LastPlayed" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.FormattedLastPlayedTime, Mode=OneWay}"/>
                            </Grid>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="20"
                                Spacing="16">
                        <TextBlock Text="游玩统计"
                                   x:Uid="WorldOverview_Statistics"
                                   Style="{StaticResource BodyStrongTextBlockStyle}"/>

                        <StackPanel Spacing="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="游玩时长" x:Uid="WorldOverview_PlayTime" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE121;" FontSize="16"/>
                                    <TextBlock Text="{x:Bind ViewModel.PlayTime, Mode=OneWay}" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="游戏内天数" x:Uid="WorldOverview_InGameDays" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE787;" FontSize="16"/>
                                    <TextBlock Text="{x:Bind ViewModel.McDays, Mode=OneWay}" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Grid>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="允许命令" x:Uid="WorldOverview_AllowCommands" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE756;" FontSize="16"/>
                                    <TextBlock Text="{x:Bind ViewModel.AllowCommands, Mode=OneWay}" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                    </Grid>
                </Grid>

                <Grid Visibility="{x:Bind ViewModel.IsDataPacksTabSelected, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <animations:Implicit.ShowAnimations>
                        <animations:TranslationAnimation Duration="0:0:0.5" From="0, 40, 0" To="0" EasingMode="EaseOut" />
                        <animations:OpacityAnimation Duration="0:0:0.5" From="0" To="1.0" />
                    </animations:Implicit.ShowAnimations>
                    <Frame x:Name="DataPacksFrame"
                           Background="Transparent"/>
                </Grid>
            </StackPanel>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
