<Page
  x:Class="XianYuLauncher.Features.ResourceDownload.Views.ResourceDownloadRootPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:helpers="using:XianYuLauncher.Helpers"
  xmlns:models="using:XianYuLauncher.Core.Models"
  xmlns:animations="using:CommunityToolkit.WinUI.Animations"
  xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
  xmlns:media="using:CommunityToolkit.WinUI.Media"
  xmlns:mediaAnimation="using:Microsoft.UI.Xaml.Media.Animation"
  xmlns:localControls="using:XianYuLauncher.Controls"
  xmlns:tabs="using:XianYuLauncher.Features.ResourceDownload.Views.Tabs"
  NavigationCacheMode="Required"
  mc:Ignorable="d">

  <Page.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Features/ResourceDownload/Views/ResourceDownloadTabResources.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Page.Resources>

  <Grid x:Name="ContentArea"
      mediaAnimation:EntranceNavigationTransitionInfo.IsTargetElement="True">
    <Grid x:Name="ResourceDownloadRootContentHost"
        MaxWidth="1064"
        HorizontalAlignment="Center"
      VerticalAlignment="Stretch">
      <TabView
        x:Name="ResourceTabView"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        Height="Auto"
        SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}"
        SelectionChanged="ResourceTabView_SelectionChanged"
        IsAddTabButtonVisible="False"
        CanDragTabs="False"
        CanReorderTabs="False">
        <TabView.Resources>
          <Style TargetType="TabViewItem">
            <Setter Property="IsClosable" Value="False" />
          </Style>
        </TabView.Resources>

        <TabView.TabItems>
          <TabViewItem>
            <TabViewItem.Header>
              <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="" FontSize="16" />
                <TextBlock x:Uid="ResourceDownloadPage_VersionTabHeader" Text="版本下载" VerticalAlignment="Center" />
              </StackPanel>
            </TabViewItem.Header>
            <tabs:VersionDownloadTabView />
          </TabViewItem>

          <TabViewItem>
            <TabViewItem.Header>
              <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="" FontSize="16" />
                <TextBlock x:Uid="ResourceDownloadPage_ModTabHeader" Text="Mod 下载" VerticalAlignment="Center" />
              </StackPanel>
            </TabViewItem.Header>
            <tabs:ModResourceTabView x:Name="ModResourceTab" />
          </TabViewItem>

          <TabViewItem>
            <TabViewItem.Header>
              <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="" FontSize="16" />
                <TextBlock x:Uid="ResourceDownloadPage_ShaderPackTabHeader" Text="光影下载" VerticalAlignment="Center" />
              </StackPanel>
            </TabViewItem.Header>
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid Grid.Row="0" Margin="20,10,20,20">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <AutoSuggestBox
                  x:Name="ShaderPackSearchBox"
                  x:Uid="ResourceDownloadPage_ShaderPackSearchBox"
                  Grid.Row="0"
                  Grid.Column="0"
                  QueryIcon="Find"
                  Margin="0,0,8,0"
                  Text="{Binding ShaderPackSearchQuery, Mode=TwoWay}"
                  QuerySubmitted="ShaderPackSearchBox_QuerySubmitted" />

                <DropDownButton
                  x:Name="ShaderPackFilterButton"
                  Grid.Row="0"
                  Grid.Column="1"
                  Margin="0,0,8,0"
                  HorizontalAlignment="Stretch"
                  HorizontalContentAlignment="Left">
                  <DropDownButton.Content>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                      <FontIcon Glyph="&#xE71C;" FontSize="12" />
                      <TextBlock x:Uid="ResourceDownloadPage_UnifiedFilterLabel" Text="筛选" />
                    </StackPanel>
                  </DropDownButton.Content>
                  <DropDownButton.Flyout>
                    <Flyout x:Name="ShaderPackFilterFlyout" Placement="Bottom"
                        Opening="ShaderPackFilterFlyout_Opening"
                        Closed="ShaderPackFilterFlyout_Closed">
                      <localControls:ResourceFilterFlyout
                        x:Name="ShaderPackFilterControl"
                        SelectionChanged="ResourceFilterControl_SelectionChanged"
                        ShowAllVersionsChanged="ResourceFilterControl_ShowAllVersionsChanged" />
                    </Flyout>
                  </DropDownButton.Flyout>
                </DropDownButton>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="0">
                  <ToggleButton
                    x:Name="ShaderPackModrinthToggle"
                    x:Uid="ResourceDownloadPage_ModrinthToggle"
                    IsChecked="{Binding IsModrinthEnabled, Mode=TwoWay}"
                    Click="ShaderPackModrinthToggleButton_Click"
                    CornerRadius="4,0,0,4"
                    Padding="10,6"
                    MinWidth="80">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <FontIcon Glyph="&#xE774;" FontSize="12" />
                      <TextBlock Text="Modrinth" FontSize="12" />
                    </StackPanel>
                  </ToggleButton>
                  <ToggleButton
                    x:Name="ShaderPackCurseForgeToggle"
                    x:Uid="ResourceDownloadPage_CurseForgeToggle"
                    IsChecked="{Binding IsCurseForgeEnabled, Mode=TwoWay}"
                    Click="ShaderPackCurseForgeToggleButton_Click"
                    CornerRadius="0,4,4,0"
                    Padding="10,6"
                    MinWidth="80">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <FontIcon Glyph="&#xE945;" FontSize="12" />
                      <TextBlock Text="CurseForge" FontSize="12" />
                    </StackPanel>
                  </ToggleButton>
                </StackPanel>
              </Grid>

              <ProgressRing
                Grid.Row="2"
                Margin="20"
                IsActive="{Binding IsShaderPackProcessing, Mode=OneWay}"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Visibility="{Binding IsShaderPackProcessing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

              <ScrollViewer
                x:Name="ShaderPackListScrollViewer"
                Grid.Row="2"
                Margin="20"
                Padding="0,0,12,0"
                VerticalScrollBarVisibility="Auto">
                <controls:AdaptiveGridView
                  x:Name="ShaderPackListView"
                  SelectionMode="Single"
                  ItemsSource="{Binding ShaderPacks, Mode=OneWay}"
                  CanDragItems="True"
                  DragItemsStarting="CommunityListView_DragItemsStarting"
                  CanReorderItems="True"
                  AllowDrop="True"
                  IsItemClickEnabled="True"
                  ItemClick="ShaderPackListView_ItemClick"
                  DesiredWidth="260"
                  ItemHeight="280"
                  StretchContentForSingleRow="False"
                  OneRowModeEnabled="False"
                  ItemContainerStyle="{StaticResource CardGridViewItemStyle}"
                  Visibility="{Binding IsShaderPackProcessing, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                  <controls:AdaptiveGridView.ItemTemplate>
                    <DataTemplate x:DataType="models:ModrinthProject">
                        <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          CornerRadius="8" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1">
                        <Grid.RowDefinitions>
                          <RowDefinition Height="140" />
                          <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" CornerRadius="8,8,0,0" Background="{ThemeResource LayerFillColorDefaultBrush}">
                          <Grid>
                            <Image Source="{Binding IconUrl, Mode=OneWay}" Stretch="UniformToFill" />
                            <Border>
                              <Border.Background>
                                <media:BackdropBlurBrush Amount="10" />
                              </Border.Background>
                            </Border>
                            <Rectangle Fill="{ThemeResource SolidBackgroundFillColorBaseAltBrush}" Opacity="0.4" />
                            <Image Source="{Binding FeaturedGallery, Mode=OneWay, TargetNullValue={x:Null}}" Stretch="UniformToFill" />
                          </Grid>
                        </Border>

                        <Grid Grid.Row="1" Padding="12">
                          <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                          </Grid.RowDefinitions>

                          <Grid Grid.Row="0" ColumnSpacing="8" Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="24" />
                              <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border CornerRadius="4" Width="24" Height="24">
                              <Image Source="{Binding IconUrl, Mode=OneWay}" Stretch="UniformToFill" />
                            </Border>
                            <TextBlock x:Name="ShaderTitleText" Grid.Column="1" Text="{x:Bind DisplayTitle, Mode=OneTime}"
                                   FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center">
                              <ToolTipService.ToolTip>
                                <ToolTip Content="{x:Bind DisplayTitle, Mode=OneTime}"
                                     IsEnabled="{x:Bind ShaderTitleText.IsTextTrimmed, Mode=OneWay}" />
                              </ToolTipService.ToolTip>
                            </TextBlock>
                          </Grid>

                          <TextBlock Grid.Row="1" Text="{Binding DisplayDescription, Mode=OneWay}"
                                 Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.8"
                                 TextWrapping="Wrap" MaxLines="2" TextTrimming="CharacterEllipsis"
                                 VerticalAlignment="Top" />

                          <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom">
                            <TextBlock Text="{Binding Author, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.6" TextTrimming="CharacterEllipsis" MaxWidth="100" />
                            <TextBlock Text="·" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.6" />
                            <TextBlock Text="{Binding Downloads, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.6" />
                          </StackPanel>
                        </Grid>
                      </Grid>
                    </DataTemplate>
                  </controls:AdaptiveGridView.ItemTemplate>
                  <controls:AdaptiveGridView.Footer>
                    <ProgressRing
                      IsActive="{Binding IsShaderPackLoadingMore, Mode=OneWay}"
                      Width="32"
                      Height="32"
                      Margin="0,10,0,10"
                      HorizontalAlignment="Center"
                      Visibility="{Binding IsShaderPackLoadingMore, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                  </controls:AdaptiveGridView.Footer>
                </controls:AdaptiveGridView>
              </ScrollViewer>
            </Grid>
          </TabViewItem>

          <TabViewItem>
            <TabViewItem.Header>
              <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE8B7;" FontSize="16" />
                <TextBlock x:Uid="ResourceDownloadPage_ResourcePackTabHeader" Text="资源包下载" VerticalAlignment="Center" />
              </StackPanel>
            </TabViewItem.Header>
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid Grid.Row="0" Margin="20,10,20,20">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <AutoSuggestBox
                  x:Name="ResourcePackSearchBox"
                  x:Uid="ResourceDownloadPage_ResourcePackSearchBox"
                  Grid.Row="0"
                  Grid.Column="0"
                  QueryIcon="Find"
                  Margin="0,0,8,0"
                  Text="{Binding ResourcePackSearchQuery, Mode=TwoWay}"
                  QuerySubmitted="ResourcePackSearchBox_QuerySubmitted" />

                <DropDownButton
                  x:Name="ResourcePackFilterButton"
                  Grid.Row="0"
                  Grid.Column="1"
                  Margin="0,0,8,0"
                  HorizontalAlignment="Stretch"
                  HorizontalContentAlignment="Left">
                  <DropDownButton.Content>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                      <FontIcon Glyph="&#xE71C;" FontSize="12" />
                      <TextBlock x:Uid="ResourceDownloadPage_UnifiedFilterLabel" Text="筛选" />
                    </StackPanel>
                  </DropDownButton.Content>
                  <DropDownButton.Flyout>
                    <Flyout x:Name="ResourcePackFilterFlyout" Placement="Bottom"
                        Opening="ResourcePackFilterFlyout_Opening"
                        Closed="ResourcePackFilterFlyout_Closed">
                      <localControls:ResourceFilterFlyout
                        x:Name="ResourcePackFilterControl"
                        SelectionChanged="ResourceFilterControl_SelectionChanged"
                        ShowAllVersionsChanged="ResourceFilterControl_ShowAllVersionsChanged" />
                    </Flyout>
                  </DropDownButton.Flyout>
                </DropDownButton>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="0">
                  <ToggleButton
                    x:Name="ResourcePackModrinthToggle"
                    x:Uid="ResourceDownloadPage_ModrinthToggle"
                    IsChecked="{Binding IsModrinthEnabled, Mode=TwoWay}"
                    Click="ResourcePackModrinthToggleButton_Click"
                    CornerRadius="4,0,0,4"
                    Padding="10,6"
                    MinWidth="80">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <FontIcon Glyph="&#xE774;" FontSize="12" />
                      <TextBlock Text="Modrinth" FontSize="12" />
                    </StackPanel>
                  </ToggleButton>
                  <ToggleButton
                    x:Name="ResourcePackCurseForgeToggle"
                    x:Uid="ResourceDownloadPage_CurseForgeToggle"
                    IsChecked="{Binding IsCurseForgeEnabled, Mode=TwoWay}"
                    Click="ResourcePackCurseForgeToggleButton_Click"
                    CornerRadius="0,4,4,0"
                    Padding="10,6"
                    MinWidth="80">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <FontIcon Glyph="&#xE945;" FontSize="12" />
                      <TextBlock Text="CurseForge" FontSize="12" />
                    </StackPanel>
                  </ToggleButton>
                </StackPanel>
              </Grid>

              <ProgressRing
                Grid.Row="2"
                Margin="20"
                IsActive="{Binding IsResourcePackProcessing, Mode=OneWay}"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Visibility="{Binding IsResourcePackProcessing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

              <ScrollViewer
                x:Name="ResourcePackListScrollViewer"
                Grid.Row="2"
                Margin="20"
                Padding="0,0,12,0"
                VerticalScrollBarVisibility="Auto">
                <controls:AdaptiveGridView
                  x:Name="ResourcePackListView"
                  SelectionMode="Single"
                  ItemsSource="{Binding ResourcePacks, Mode=OneWay}"
                  DragItemsStarting="CommunityListView_DragItemsStarting"
                  CanDragItems="True"
                  CanReorderItems="True"
                  AllowDrop="True"
                  IsItemClickEnabled="True"
                  ItemClick="ResourcePackListView_ItemClick"
                  DesiredWidth="260"
                  ItemHeight="280"
                  StretchContentForSingleRow="False"
                  OneRowModeEnabled="False"
                  ItemContainerStyle="{StaticResource CardGridViewItemStyle}"
                  Visibility="{Binding IsResourcePackProcessing, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                  <controls:AdaptiveGridView.ItemTemplate>
                    <DataTemplate x:DataType="models:ModrinthProject">
                        <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          CornerRadius="8" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1">
                        <Grid.RowDefinitions>
                          <RowDefinition Height="140" />
                          <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" CornerRadius="8,8,0,0" Background="{ThemeResource LayerFillColorDefaultBrush}">
                          <Grid>
                            <Image Source="{Binding IconUrl, Mode=OneWay}" Stretch="UniformToFill" />
                            <Border>
                              <Border.Background>
                                <media:BackdropBlurBrush Amount="10" />
                              </Border.Background>
                            </Border>
                            <Rectangle Fill="{ThemeResource SolidBackgroundFillColorBaseAltBrush}" Opacity="0.4" />
                            <Image Source="{Binding FeaturedGallery, Mode=OneWay, TargetNullValue={x:Null}}" Stretch="UniformToFill" />
                          </Grid>
                        </Border>

                        <Grid Grid.Row="1" Padding="12">
                          <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                          </Grid.RowDefinitions>

                          <Grid Grid.Row="0" ColumnSpacing="8" Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                              <ColumnDefinition Width="24" />
                              <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border CornerRadius="4" Width="24" Height="24">
                              <Image Source="{Binding IconUrl, Mode=OneWay}" Stretch="UniformToFill" />
                            </Border>
                            <TextBlock x:Name="ResourcePackTitleText" Grid.Column="1" Text="{x:Bind DisplayTitle, Mode=OneTime}"
                                   FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center">
                              <ToolTipService.ToolTip>
                                <ToolTip Content="{x:Bind DisplayTitle, Mode=OneTime}"
                                     IsEnabled="{x:Bind ResourcePackTitleText.IsTextTrimmed, Mode=OneWay}" />
                              </ToolTipService.ToolTip>
                            </TextBlock>
                          </Grid>

                          <TextBlock Grid.Row="1" Text="{Binding DisplayDescription, Mode=OneWay}"
                                 Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.8"
                                 TextWrapping="Wrap" MaxLines="2" TextTrimming="CharacterEllipsis"
                                 VerticalAlignment="Top" />

                          <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom">
                            <TextBlock Text="{Binding Author, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.6" TextTrimming="CharacterEllipsis" MaxWidth="100" />
                            <TextBlock Text="·" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.6" />
                            <TextBlock Text="{Binding Downloads, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.6" />
                          </StackPanel>
                        </Grid>
                      </Grid>
                    </DataTemplate>
                  </controls:AdaptiveGridView.ItemTemplate>
                  <controls:AdaptiveGridView.Footer>
                    <ProgressRing
                      IsActive="{Binding IsResourcePackLoadingMore, Mode=OneWay}"
                      Width="32"
                      Height="32"
                      Margin="0,10,0,10"
                      HorizontalAlignment="Center"
                      Visibility="{Binding IsResourcePackLoadingMore, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                  </controls:AdaptiveGridView.Footer>
                </controls:AdaptiveGridView>
              </ScrollViewer>
            </Grid>
          </TabViewItem>

          <TabViewItem>
            <TabViewItem.Header>
              <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE760;" FontSize="16" />
                <TextBlock x:Uid="ResourceDownloadPage_DatapackTabHeader" Text="数据包下载" VerticalAlignment="Center" />
              </StackPanel>
            </TabViewItem.Header>
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid Grid.Row="0" Margin="20,10,20,20">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <AutoSuggestBox
                  x:Name="DatapackSearchBox"
                  x:Uid="ResourceDownloadPage_DatapackSearchBox"
                  Grid.Row="0"
                  Grid.Column="0"
                  PlaceholderText="Search datapacks..."
                  QueryIcon="Find"
                  Margin="0,0,8,0"
                  Text="{Binding DatapackSearchQuery, Mode=TwoWay}"
                  QuerySubmitted="DatapackSearchBox_QuerySubmitted" />

                <DropDownButton
                  x:Name="DatapackFilterButton"
                  Grid.Row="0"
                  Grid.Column="1"
                  Margin="0,0,8,0"
                  HorizontalAlignment="Stretch"
                  HorizontalContentAlignment="Left">
                  <DropDownButton.Content>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                      <FontIcon Glyph="&#xE71C;" FontSize="12" />
                      <TextBlock x:Uid="ResourceDownloadPage_UnifiedFilterLabel" Text="筛选" />
                    </StackPanel>
                  </DropDownButton.Content>
                  <DropDownButton.Flyout>
                    <Flyout x:Name="DatapackFilterFlyout" Placement="Bottom"
                        Opening="DatapackFilterFlyout_Opening"
                        Closed="DatapackFilterFlyout_Closed">
                      <localControls:ResourceFilterFlyout
                        x:Name="DatapackFilterControl"
                        SelectionChanged="ResourceFilterControl_SelectionChanged"
                        ShowAllVersionsChanged="ResourceFilterControl_ShowAllVersionsChanged" />
                    </Flyout>
                  </DropDownButton.Flyout>
                </DropDownButton>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="0">
                  <ToggleButton
                    x:Name="DatapackModrinthToggle"
                    x:Uid="ResourceDownloadPage_ModrinthToggle"
                    IsChecked="{Binding IsModrinthEnabled, Mode=TwoWay}"
                    Click="DatapackModrinthToggleButton_Click"
                    CornerRadius="4,0,0,4"
                    Padding="10,6"
                    MinWidth="80">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <FontIcon Glyph="&#xE774;" FontSize="12" />
                      <TextBlock Text="Modrinth" FontSize="12" />
                    </StackPanel>
                  </ToggleButton>
                  <ToggleButton
                    x:Name="DatapackCurseForgeToggle"
                    x:Uid="ResourceDownloadPage_CurseForgeToggle"
                    IsChecked="{Binding IsCurseForgeEnabled, Mode=TwoWay}"
                    Click="DatapackCurseForgeToggleButton_Click"
                    CornerRadius="0,4,4,0"
                    Padding="10,6"
                    MinWidth="80">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <FontIcon Glyph="&#xE945;" FontSize="12" />
                      <TextBlock Text="CurseForge" FontSize="12" />
                    </StackPanel>
                  </ToggleButton>
                </StackPanel>
              </Grid>

              <ProgressRing
                Grid.Row="2"
                Margin="20"
                IsActive="{Binding IsDatapackProcessing, Mode=OneWay}"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Visibility="{Binding IsDatapackProcessing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

              <ScrollViewer
                x:Name="DatapackListScrollViewer"
                Grid.Row="2"
                Margin="20"
                Padding="0,0,12,0"
                VerticalScrollBarVisibility="Auto">
                <ListView
                  x:Name="DatapackListView"
                  SelectionMode="Single"
                  ItemsSource="{Binding Datapacks, Mode=OneWay}"
                  DragItemsStarting="CommunityListView_DragItemsStarting"
                  CanDragItems="True"
                  CanReorderItems="True"
                  AllowDrop="True"
                  IsItemClickEnabled="True"
                  ItemClick="DatapackListView_ItemClick"
                  Visibility="{Binding IsDatapackProcessing, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                  <ListView.Resources>
                    <Style TargetType="ScrollViewer">
                      <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
                    </Style>
                  </ListView.Resources>
                  <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                      <VirtualizingStackPanel VirtualizationMode="Recycling" />
                    </ItemsPanelTemplate>
                  </ListView.ItemsPanel>
                  <ListView.ItemContainerStyle>
                    <StaticResource ResourceKey="CommunityResourceListViewItemStyle" />
                  </ListView.ItemContainerStyle>
                  <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:ModrinthProject">
                        <Grid Height="120" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          CornerRadius="8" Padding="12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="Auto" />
                          <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                          <RowDefinition Height="Auto" />
                          <RowDefinition Height="*" />
                          <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" Width="64" Height="64" Margin="0,0,12,0"
                            CornerRadius="32" Background="Transparent">
                          <Image Stretch="UniformToFill">
                            <Image.Source>
                              <BitmapImage UriSource="{Binding IconUrl, Mode=OneWay}" DecodePixelWidth="64" DecodePixelHeight="64" />
                            </Image.Source>
                          </Image>
                        </Border>

                        <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding DisplayTitle, Mode=OneWay}"
                               FontSize="16" FontWeight="SemiBold" />

                        <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding DisplayDescription, Mode=OneWay}"
                               Style="{ThemeResource BodyTextBlockStyle}" Opacity="0.8" TextWrapping="WrapWholeWords"
                               MaxLines="2" VerticalAlignment="Center" />

                        <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom">
                          <TextBlock Text="{Binding Author, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7" />
                          <TextBlock Text="{Binding LatestSupportedVersion, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7" />
                          <TextBlock Text="{Binding Downloads, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7" />
                        </StackPanel>
                      </Grid>
                    </DataTemplate>
                  </ListView.ItemTemplate>
                  <ListView.Footer>
                    <ProgressRing
                      IsActive="{Binding IsDatapackLoadingMore, Mode=OneWay}"
                      Width="32"
                      Height="32"
                      Margin="0,10,0,10"
                      HorizontalAlignment="Center"
                      Visibility="{Binding IsDatapackLoadingMore, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                  </ListView.Footer>
                </ListView>
              </ScrollViewer>
            </Grid>
          </TabViewItem>

          <TabViewItem>
            <TabViewItem.Header>
              <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="" FontSize="16" />
                <TextBlock x:Uid="ResourceDownloadPage_ModpackTabHeader" Text="整合包下载" VerticalAlignment="Center" />
              </StackPanel>
            </TabViewItem.Header>
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid Grid.Row="0" Margin="20,10,20,20">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <AutoSuggestBox
                  x:Name="ModpackSearchBox"
                  x:Uid="ResourceDownloadPage_ModpackSearchBox"
                  Grid.Row="0"
                  Grid.Column="0"
                  PlaceholderText="Search modpacks..."
                  QueryIcon="Find"
                  Margin="0,0,8,0"
                  Text="{Binding ModpackSearchQuery, Mode=TwoWay}"
                  QuerySubmitted="ModpackSearchBox_QuerySubmitted" />

                <DropDownButton
                  x:Name="ModpackFilterButton"
                  Grid.Row="0"
                  Grid.Column="1"
                  Margin="0,0,8,0"
                  HorizontalAlignment="Stretch"
                  HorizontalContentAlignment="Left">
                  <DropDownButton.Content>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                      <FontIcon Glyph="&#xE71C;" FontSize="12" />
                      <TextBlock x:Uid="ResourceDownloadPage_UnifiedFilterLabel" Text="筛选" />
                    </StackPanel>
                  </DropDownButton.Content>
                  <DropDownButton.Flyout>
                    <Flyout x:Name="ModpackFilterFlyout" Placement="Bottom"
                        Opening="ModpackFilterFlyout_Opening"
                        Closed="ModpackFilterFlyout_Closed">
                      <localControls:ResourceFilterFlyout
                        x:Name="ModpackFilterControl"
                        SelectionChanged="ResourceFilterControl_SelectionChanged"
                        ShowAllVersionsChanged="ResourceFilterControl_ShowAllVersionsChanged" />
                    </Flyout>
                  </DropDownButton.Flyout>
                </DropDownButton>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="0">
                  <ToggleButton
                    x:Name="ModpackModrinthToggle"
                    x:Uid="ResourceDownloadPage_ModrinthToggle"
                    IsChecked="{Binding IsModrinthEnabled, Mode=TwoWay}"
                    Click="ModpackModrinthToggleButton_Click"
                    CornerRadius="4,0,0,4"
                    Padding="10,6"
                    MinWidth="80">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <FontIcon Glyph="&#xE774;" FontSize="12" />
                      <TextBlock Text="Modrinth" FontSize="12" />
                    </StackPanel>
                  </ToggleButton>
                  <ToggleButton
                    x:Name="ModpackCurseForgeToggle"
                    x:Uid="ResourceDownloadPage_CurseForgeToggle"
                    IsChecked="{Binding IsCurseForgeEnabled, Mode=TwoWay}"
                    Click="ModpackCurseForgeToggleButton_Click"
                    CornerRadius="0,4,4,0"
                    Padding="10,6"
                    MinWidth="80">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <FontIcon Glyph="&#xE945;" FontSize="12" />
                      <TextBlock Text="CurseForge" FontSize="12" />
                    </StackPanel>
                  </ToggleButton>
                </StackPanel>
              </Grid>

              <ProgressRing
                Grid.Row="2"
                Margin="20"
                IsActive="{Binding IsModpackProcessing, Mode=OneWay}"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Visibility="{Binding IsModpackProcessing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

              <ScrollViewer
                x:Name="ModpackListScrollViewer"
                Grid.Row="2"
                Margin="20"
                Padding="0,0,12,0"
                VerticalScrollBarVisibility="Auto">
                <ListView
                  x:Name="ModpackListView"
                  SelectionMode="Single"
                  DragItemsStarting="CommunityListView_DragItemsStarting"
                  ItemsSource="{Binding Modpacks, Mode=OneWay}"
                  CanDragItems="True"
                  CanReorderItems="True"
                  AllowDrop="True"
                  IsItemClickEnabled="True"
                  ItemClick="ModpackListView_ItemClick"
                  Visibility="{Binding IsModpackProcessing, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                  <ListView.Resources>
                    <Style TargetType="ScrollViewer">
                      <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
                    </Style>
                  </ListView.Resources>
                  <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                      <VirtualizingStackPanel VirtualizationMode="Recycling" />
                    </ItemsPanelTemplate>
                  </ListView.ItemsPanel>
                  <ListView.ItemContainerStyle>
                    <StaticResource ResourceKey="CommunityResourceListViewItemStyle" />
                  </ListView.ItemContainerStyle>
                  <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:ModrinthProject">
                        <Grid Height="120" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          CornerRadius="8" Padding="12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="Auto" />
                          <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                          <RowDefinition Height="Auto" />
                          <RowDefinition Height="*" />
                          <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" Width="64" Height="64" Margin="0,0,12,0"
                            CornerRadius="32" Background="Transparent">
                          <Image Stretch="UniformToFill">
                            <Image.Source>
                              <BitmapImage UriSource="{Binding IconUrl, Mode=OneWay}" DecodePixelWidth="64" DecodePixelHeight="64" />
                            </Image.Source>
                          </Image>
                        </Border>

                        <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding DisplayTitle, Mode=OneWay}"
                               FontSize="16" FontWeight="SemiBold" />

                        <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding DisplayDescription, Mode=OneWay}"
                               Style="{ThemeResource BodyTextBlockStyle}" Opacity="0.8" TextWrapping="WrapWholeWords"
                               MaxLines="2" VerticalAlignment="Center" />

                        <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom">
                          <TextBlock Text="{Binding Author, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7" />
                          <TextBlock Text="{Binding LatestSupportedVersion, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7" />
                          <TextBlock Text="{Binding Downloads, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7" />
                        </StackPanel>
                      </Grid>
                    </DataTemplate>
                  </ListView.ItemTemplate>
                  <ListView.Footer>
                    <ProgressRing
                      IsActive="{Binding IsModpackLoadingMore, Mode=OneWay}"
                      Width="32"
                      Height="32"
                      Margin="0,10,0,10"
                      HorizontalAlignment="Center"
                      Visibility="{Binding IsModpackLoadingMore, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                  </ListView.Footer>
                </ListView>
              </ScrollViewer>
            </Grid>
          </TabViewItem>

          <TabViewItem>
            <TabViewItem.Header>
              <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE909;" FontSize="16" />
                <TextBlock x:Uid="ResourceDownloadPage_WorldTabHeader" Text="世界" VerticalAlignment="Center" />
              </StackPanel>
            </TabViewItem.Header>
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>

              <Grid Grid.Row="0" Margin="20,10,20,20">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <AutoSuggestBox
                  x:Name="WorldSearchBox"
                  x:Uid="ResourceDownloadPage_WorldSearchBox"
                  Grid.Row="0"
                  Grid.Column="0"
                  PlaceholderText="Search worlds..."
                  QueryIcon="Find"
                  Margin="0,0,8,0"
                  Text="{Binding WorldSearchQuery, Mode=TwoWay}"
                  QuerySubmitted="WorldSearchBox_QuerySubmitted" />

                <DropDownButton
                  x:Name="WorldFilterButton"
                  Grid.Row="0"
                  Grid.Column="1"
                  Margin="0,0,8,0"
                  HorizontalAlignment="Stretch"
                  HorizontalContentAlignment="Left">
                  <DropDownButton.Content>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                      <FontIcon Glyph="&#xE71C;" FontSize="12" />
                      <TextBlock x:Uid="ResourceDownloadPage_UnifiedFilterLabel" Text="筛选" />
                    </StackPanel>
                  </DropDownButton.Content>
                  <DropDownButton.Flyout>
                    <Flyout x:Name="WorldFilterFlyout" Placement="Bottom"
                        Opening="WorldFilterFlyout_Opening"
                        Closed="WorldFilterFlyout_Closed">
                      <localControls:ResourceFilterFlyout
                        x:Name="WorldFilterControl"
                        SelectionChanged="ResourceFilterControl_SelectionChanged"
                        ShowAllVersionsChanged="ResourceFilterControl_ShowAllVersionsChanged" />
                    </Flyout>
                  </DropDownButton.Flyout>
                </DropDownButton>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="0">
                  <ToggleButton
                    x:Name="WorldModrinthToggle"
                    x:Uid="ResourceDownloadPage_ModrinthToggle"
                    IsChecked="False"
                    IsEnabled="False"
                    Click="WorldModrinthToggleButton_Click"
                    CornerRadius="4,0,0,4"
                    Padding="10,6"
                    MinWidth="80"
                    ToolTipService.ToolTip="Modrinth 不支持世界下载">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <FontIcon Glyph="&#xE774;" FontSize="12" />
                      <TextBlock Text="Modrinth" FontSize="12" />
                    </StackPanel>
                  </ToggleButton>
                  <ToggleButton
                    x:Name="WorldCurseForgeToggle"
                    x:Uid="ResourceDownloadPage_CurseForgeToggle"
                    IsChecked="{Binding IsCurseForgeEnabled, Mode=TwoWay}"
                    Click="WorldCurseForgeToggleButton_Click"
                    CornerRadius="0,4,4,0"
                    Padding="10,6"
                    MinWidth="80">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                      <FontIcon Glyph="&#xE945;" FontSize="12" />
                      <TextBlock Text="CurseForge" FontSize="12" />
                    </StackPanel>
                  </ToggleButton>
                </StackPanel>
              </Grid>

              <ProgressRing
                Grid.Row="2"
                Margin="20"
                IsActive="{Binding IsWorldProcessing, Mode=OneWay}"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Visibility="{Binding IsWorldProcessing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

              <ScrollViewer
                x:Name="WorldListScrollViewer"
                Grid.Row="2"
                Margin="20"
                Padding="0,0,12,0"
                VerticalScrollBarVisibility="Auto">
                <ListView
                  x:Name="WorldListView"
                  SelectionMode="Single"
                  DragItemsStarting="CommunityListView_DragItemsStarting"
                  ItemsSource="{Binding Worlds, Mode=OneWay}"
                  CanDragItems="True"
                  CanReorderItems="True"
                  AllowDrop="True"
                  IsItemClickEnabled="True"
                  ItemClick="WorldListView_ItemClick"
                  Visibility="{Binding IsWorldProcessing, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                  <ListView.Resources>
                    <Style TargetType="ScrollViewer">
                      <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
                    </Style>
                  </ListView.Resources>
                  <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                      <VirtualizingStackPanel VirtualizationMode="Recycling" />
                    </ItemsPanelTemplate>
                  </ListView.ItemsPanel>
                  <ListView.ItemContainerStyle>
                    <StaticResource ResourceKey="CommunityResourceListViewItemStyle" />
                  </ListView.ItemContainerStyle>
                  <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:ModrinthProject">
                        <Grid Height="120" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          CornerRadius="8" Padding="12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                          BorderThickness="1">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="Auto" />
                          <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                          <RowDefinition Height="Auto" />
                          <RowDefinition Height="*" />
                          <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" Width="64" Height="64" Margin="0,0,12,0"
                            CornerRadius="32" Background="Transparent">
                          <Image Stretch="UniformToFill">
                            <Image.Source>
                              <BitmapImage UriSource="{Binding IconUrl, Mode=OneWay}" DecodePixelWidth="64" DecodePixelHeight="64" />
                            </Image.Source>
                          </Image>
                        </Border>

                        <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding DisplayTitle, Mode=OneWay}"
                               FontSize="16" FontWeight="SemiBold" />

                        <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding DisplayDescription, Mode=OneWay}"
                               Style="{ThemeResource BodyTextBlockStyle}" Opacity="0.8" TextWrapping="WrapWholeWords"
                               MaxLines="2" VerticalAlignment="Center" />

                        <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom">
                          <TextBlock Text="{Binding Author, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7" />
                          <TextBlock Text="{Binding LatestSupportedVersion, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7" />
                          <TextBlock Text="{Binding Downloads, Mode=OneWay}" Style="{ThemeResource CaptionTextBlockStyle}" Opacity="0.7" />
                        </StackPanel>
                      </Grid>
                    </DataTemplate>
                  </ListView.ItemTemplate>
                  <ListView.Footer>
                    <ProgressRing
                      IsActive="{Binding IsWorldLoadingMore, Mode=OneWay}"
                      Width="32"
                      Height="32"
                      Margin="0,10,0,10"
                      HorizontalAlignment="Center"
                      Visibility="{Binding IsWorldLoadingMore, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                  </ListView.Footer>
                </ListView>
              </ScrollViewer>
            </Grid>
          </TabViewItem>
        </TabView.TabItems>
      </TabView>
    </Grid>
  </Grid>
</Page>