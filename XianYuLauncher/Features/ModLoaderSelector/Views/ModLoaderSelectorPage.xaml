<Page
    x:Class="XianYuLauncher.Features.ModLoaderSelector.Views.ModLoaderSelectorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:XianYuLauncher.Helpers"
    xmlns:core="using:XianYuLauncher.Core.Models"
    xmlns:controls="using:XianYuLauncher.Controls"
    mc:Ignorable="d"
    HorizontalAlignment="Stretch">

    <Page.Resources>
        <helpers:StringNullOrEmptyToBoolConverter x:Key="StringNullOrEmptyToBoolConverter" />
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <helpers:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
        <helpers:InverseBoolConverter x:Key="InverseBoolConverter" />
        <helpers:InverseBoolToOpacityConverter x:Key="InverseBoolToOpacityConverter" />
        <helpers:BoolToFontWeightConverter x:Key="BoolToFontWeightConverter" />
        <helpers:BoolToBrushConverter x:Key="BoolToBrushConverter" />
        <helpers:ModLoaderToIconConverter x:Key="ModLoaderToIconConverter" />
        <helpers:StringEqualsConverter x:Key="StringEqualsConverter" />
        <helpers:PathToImageSourceConverter x:Key="PathToImageSourceConverter" />

        <Style x:Key="VersionListViewItemStyle" TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Margin" Value="0,2,0,2" />
        </Style>
    </Page.Resources>

    <Grid x:Name="ContentArea" Padding="{StaticResource PageContentPadding}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="100*" MaxWidth="1064" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid x:Name="PageLayoutRoot" Grid.Column="1" VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 页面标题 -->
            <StackPanel Grid.Row="0"
                        Margin="0,0,0,24"
                        Spacing="8"
                        Visibility="{x:Bind ViewModel.IsEmbeddedHostNavigation, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                <TextBlock x:Uid="ModLoaderSelectionPage_TitleText" Text="下载 Minecraft" Style="{ThemeResource TitleTextBlockStyle}" />

                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <TextBlock x:Uid="ModLoaderSelectionPage_CurrentVersionText" Text="当前版本:" Style="{StaticResource BodyTextStyle}" Opacity="0.7" VerticalAlignment="Center" Margin="0,0,0,2" />

                    <DropDownButton Padding="8,4" VerticalAlignment="Center" CornerRadius="4" BorderThickness="0" Background="Transparent" Margin="-8,0,0,0">
                        <DropDownButton.Content>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Image
                                    Source="{x:Bind ViewModel.SelectedIconPath, Mode=OneWay, Converter={StaticResource PathToImageSourceConverter}}"
                                    Width="16"
                                    Height="16"
                                    Stretch="UniformToFill"
                                    VerticalAlignment="Center" />
                                <TextBlock
                                    Text="{x:Bind ViewModel.SelectedVersionDisplayText, Mode=OneWay}"
                                    Style="{StaticResource BodyStrongTextBlockStyle}"
                                    VerticalAlignment="Center"
                                    Margin="0,0,4,0" />
                            </StackPanel>
                        </DropDownButton.Content>
                        <DropDownButton.Flyout>
                            <Flyout Placement="BottomEdgeAlignedLeft">
                                <controls:VersionIconPickerFlyoutContent
                                    AvailableIcons="{x:Bind ViewModel.AvailableIcons, Mode=OneWay}"
                                    BuiltInIconSelected="VersionIconPicker_BuiltInIconSelected"
                                    CustomIconRequested="VersionIconPicker_CustomIconRequested" />
                            </Flyout>
                        </DropDownButton.Flyout>
                    </DropDownButton>
                </StackPanel>
            </StackPanel>

            <!-- 版本名称编辑卡片 -->
            <Grid Grid.Row="1" Margin="0,0,0,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock x:Uid="ModLoaderSelectionPage_VersionNameText" Text="版本名称:" Margin="0,0,0,12" FontWeight="SemiBold" FontSize="16" />
                <Border
                    Grid.Row="1"
                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                    BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="16">
                    <StackPanel>
                        <TextBlock
                            Text="{x:Bind ViewModel.VersionNameDescription, Mode=OneWay}"
                            FontSize="14"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Margin="0,0,0,8" />
                        <TextBox
                            x:Uid="ModLoaderSelectionPage_VersionNameTextBox"
                            Text="{x:Bind ViewModel.VersionName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            PlaceholderText="例如: 1.21.10-fabric-0.15.3"
                            FontSize="16"
                            Padding="12"
                            Background="{ThemeResource TextControlBackground}"
                            BorderBrush="{ThemeResource TextControlBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Mod Loader 选择区域 -->
            <Grid Grid.Row="2" Margin="0,0,0,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock x:Uid="ModLoaderSelectionPage_SelectComponentsText" Text="选择附属组件:" Margin="0,0,0,16" FontWeight="SemiBold" FontSize="16" />

                <!-- Mod Loader Expander 列表，保持全宽以让滚动条贴页面右侧 -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" HorizontalAlignment="Stretch">
                    <StackPanel Spacing="12">
                        <ItemsRepeater ItemsSource="{x:Bind ViewModel.ModLoaderItems, Mode=OneWay}" HorizontalAlignment="Stretch">
                            <ItemsRepeater.Layout>
                                <StackLayout Spacing="12" />
                            </ItemsRepeater.Layout>
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate x:DataType="core:ModLoaderItem">
                                    <Expander
                                        x:Name="ModLoaderExpander"
                                        IsExpanded="{x:Bind IsSelected, Mode=TwoWay}"
                                        BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Padding="0"
                                        RequestedTheme="Default"
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Stretch">
                                        <Expander.Header>
                                            <!-- Expander 头部承担选择入口，选中状态也在这里回显 -->
                                            <Grid
                                                Padding="16,12"
                                                CornerRadius="4 4 0 0"
                                                HorizontalAlignment="Stretch"
                                                DataContext="{x:Bind}">
                                                <Grid.Background>
                                                    <SolidColorBrush Color="Transparent" />
                                                </Grid.Background>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock
                                                    Grid.Column="0"
                                                    Text="{x:Bind Name, Mode=OneWay}"
                                                    FontSize="16"
                                                    FontWeight="{x:Bind IsSelected, Mode=OneWay, Converter={StaticResource BoolToFontWeightConverter}}"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Stretch" />
                                                <Button
                                                    x:Uid="ModLoaderSelectionPage_CancelSelectionButton"
                                                    Grid.Column="1"
                                                    Width="24"
                                                    Height="24"
                                                    Padding="0"
                                                    Margin="-8,0,0,0"
                                                    Background="Transparent"
                                                    BorderBrush="Transparent"
                                                    Foreground="{ThemeResource TextFillColorSecondary}"
                                                    Click="CancelModLoader_Click"
                                                    Tag="{Binding}"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Center"
                                                    Visibility="{x:Bind IsSelected, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                                    ToolTipService.ToolTip="取消选择">
                                                    <FontIcon Glyph="&#xE711;" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Button>
                                            </Grid>
                                        </Expander.Header>

                                        <!-- Expander 内容包含加载态和版本列表，两者复用同一块内容区域 -->
                                        <Grid Padding="16,12" MinHeight="100" HorizontalAlignment="Stretch">
                                            <ProgressRing
                                                IsActive="{x:Bind IsLoading, Mode=OneWay}"
                                                Visibility="{x:Bind IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Width="48"
                                                Height="48" />

                                            <ListView
                                                x:Name="ModLoaderListView"
                                                ItemsSource="{x:Bind Versions, Mode=OneWay}"
                                                SelectedItem="{x:Bind SelectedVersion, Mode=TwoWay}"
                                                SelectionMode="Single"
                                                Visibility="{x:Bind IsLoading, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                HorizontalAlignment="Stretch"
                                                VerticalAlignment="Stretch"
                                                MaxHeight="200"
                                                Width="Auto"
                                                ItemContainerStyle="{StaticResource VersionListViewItemStyle}"
                                                Tag="{x:Bind Mode=OneWay}">
                                                <ListView.ItemTemplate>
                                                    <DataTemplate x:DataType="x:String">
                                                        <Grid Padding="12,8" HorizontalAlignment="Stretch">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto" />
                                                                <ColumnDefinition Width="*" />
                                                            </Grid.ColumnDefinitions>

                                                            <Image
                                                                Grid.Column="0"
                                                                Source="{Binding ElementName=ModLoaderListView, Path=Tag.IconUrl}"
                                                                Width="20"
                                                                Height="20"
                                                                Margin="0,0,12,0"
                                                                VerticalAlignment="Center"
                                                                Stretch="UniformToFill"
                                                                Visibility="Visible" />

                                                            <Grid Grid.Column="1" HorizontalAlignment="Stretch">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>

                                                                <TextBlock
                                                                    Grid.Column="0"
                                                                    Text="{x:Bind}"
                                                                    FontSize="15"
                                                                    VerticalAlignment="Center"
                                                                    HorizontalAlignment="Left" />

                                                                <TextBlock
                                                                    Grid.Column="1"
                                                                    FontSize="12"
                                                                    Foreground="{ThemeResource TextFillColorSecondary}"
                                                                    VerticalAlignment="Center"
                                                                    HorizontalAlignment="Right"
                                                                    Margin="8,0,0,0"
                                                                    Tag="{x:Bind}"
                                                                    Loaded="CompatibleInfoTextBlock_Loaded" />
                                                            </Grid>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ListView.ItemTemplate>
                                            </ListView>
                                        </Grid>
                                    </Expander>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>

                        <!-- LiteLoader Expander -->
                        <Expander
                            x:Name="LiteLoaderExpander"
                            IsExpanded="{x:Bind ViewModel.IsLiteLoaderSelected, Mode=TwoWay}"
                            Visibility="{x:Bind ViewModel.IsLiteLoaderSupported, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                            BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="0"
                            RequestedTheme="Default"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch">
                            <Expander.Header>
                                <Grid
                                    Padding="16,12"
                                    CornerRadius="4 4 0 0"
                                    HorizontalAlignment="Stretch">
                                    <Grid.Background>
                                        <SolidColorBrush Color="Transparent" />
                                    </Grid.Background>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" Grid.Column="0">
                                        <TextBlock
                                            Text="LiteLoader"
                                            FontSize="16"
                                            FontWeight="{x:Bind ViewModel.IsLiteLoaderSelected, Mode=OneWay, Converter={StaticResource BoolToFontWeightConverter}}"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Stretch" />
                                    </StackPanel>
                                    <Button
                                        Grid.Column="1"
                                        Width="24"
                                        Height="24"
                                        Padding="0"
                                        Margin="-8,0,0,0"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        Foreground="{ThemeResource TextFillColorSecondary}"
                                        Click="CancelLiteLoader_Click"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Center"
                                        Visibility="{x:Bind ViewModel.IsLiteLoaderSelected, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                        ToolTipService.ToolTip="取消选择">
                                        <FontIcon Glyph="&#xE711;" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Button>
                                </Grid>
                            </Expander.Header>

                            <Grid Padding="16,12" MinHeight="100" HorizontalAlignment="Stretch">
                                <ProgressRing
                                    IsActive="{x:Bind ViewModel.IsLiteLoaderLoading, Mode=OneWay}"
                                    Visibility="{x:Bind ViewModel.IsLiteLoaderLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Width="48"
                                    Height="48" />

                                <ListView
                                    ItemsSource="{x:Bind ViewModel.LiteLoaderVersions, Mode=OneWay}"
                                    SelectedItem="{x:Bind ViewModel.SelectedLiteLoaderVersion, Mode=TwoWay}"
                                    SelectionMode="Single"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    Visibility="{x:Bind ViewModel.IsLiteLoaderLoading, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                                    ItemContainerStyle="{StaticResource VersionListViewItemStyle}"
                                    MaxHeight="200"
                                    Width="Auto">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="x:String">
                                            <Grid Padding="12,8" HorizontalAlignment="Stretch">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>

                                                <Image
                                                    Grid.Column="0"
                                                    Source="Assets/Icons/Download_Options/Liteloader/Liteloader.ico"
                                                    Width="20"
                                                    Height="20"
                                                    Margin="0,0,12,0"
                                                    VerticalAlignment="Center"
                                                    Stretch="UniformToFill"
                                                    Visibility="Visible" />

                                                <Grid Grid.Column="1" HorizontalAlignment="Stretch">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>

                                                    <TextBlock
                                                        Grid.Column="0"
                                                        Text="{x:Bind}"
                                                        FontSize="15"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Left" />
                                                </Grid>
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </Grid>
                        </Expander>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- 确认按钮 -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12">
                <Button
                    x:Uid="ModLoaderSelectionPage_CancelButton"
                    Content="取消"
                    Command="{x:Bind ViewModel.CancelCommand}"
                    MinWidth="100" />
                <Button
                    x:Uid="ModLoaderSelectionPage_ConfirmDownloadButton"
                    Content="确认下载"
                    Command="{x:Bind ViewModel.ConfirmSelectionCommand}"
                    IsEnabled="{x:Bind ViewModel.IsVersionNameValid, Mode=OneWay}"
                    MinWidth="120" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
