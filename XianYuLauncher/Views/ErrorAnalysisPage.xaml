<Page
    x:Class="XMCL2025.Views.ErrorAnalysisPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XMCL2025.Views"
    xmlns:viewModels="using:XMCL2025.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- 顶部导航栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="{ThemeResource NavigationViewExpandedPaneBackground}" Padding="16" Spacing="8" VerticalAlignment="Stretch">
            <Button Content="&lt; 返回" Click="BackButton_Click" Style="{StaticResource TextBlockButtonStyle}" VerticalAlignment="Center" />
            <TextBlock Text="错误分析系统" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="16,0,0,0" />
        </StackPanel>
        
        <!-- 功能按钮区 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="16,10,16,10" Spacing="8" VerticalAlignment="Center">
            <Button Content="导出错误日志" Command="{x:Bind ViewModel.ExportErrorLogsCommand}" Width="120" Height="36" />
            <Button Content="复制日志" Command="{x:Bind ViewModel.CopyLogsCommand}" Width="120" Height="36" />
            <Button Content="清空日志" Command="{x:Bind ViewModel.ClearLogsCommand}" Width="120" Height="36" />
        </StackPanel>
        
        <!-- 日志显示区 -->
        <Grid Grid.Row="2" Margin="16,0,16,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <TextBlock Text="错误日志详情" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8" />
            
            <TextBlock Grid.Row="1" 
                       FontSize="14" 
                       Foreground="{ThemeResource TextFillColorPrimary}" 
                       Margin="0,0,0,8">
                <Run Text="崩溃原因: "/>
                <Run Text="{x:Bind ViewModel.CrashReason, Mode=OneWay}" FontWeight="SemiBold" />
            </TextBlock>
            
            <ScrollViewer Grid.Row="2" 
                           VerticalScrollBarVisibility="Auto" 
                           HorizontalScrollBarVisibility="Auto" 
                           Background="{ThemeResource TextControlBackground}" 
                           BorderBrush="{ThemeResource TextControlBorderBrush}" 
                           BorderThickness="1" 
                           CornerRadius="4" 
                           Padding="8">
                <TextBox Text="{x:Bind ViewModel.FullLog, Mode=OneWay}" 
                         FontFamily="Consolas" 
                         FontSize="12" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True" 
                         IsReadOnly="True" 
                         Background="Transparent" 
                         BorderBrush="Transparent" 
                         BorderThickness="0" />
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>