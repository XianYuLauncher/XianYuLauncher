# Scripts

开发辅助脚本。

## 开发用

### run.ps1

WinUI 3 打包应用的开发运行脚本，采用与 Visual Studio F5 一致的松散注册策略

**用法**（在仓库根目录执行）：

```powershell
.\scripts\run.ps1
```

**参数**：

- `-NoLaunch`：仅构建并注册，不启动应用

---

## CI 用

None...
