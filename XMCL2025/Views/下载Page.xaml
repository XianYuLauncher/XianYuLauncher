<Page
    x:Class="XMCL2025.Views.下载Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:XMCL2025.ViewModels"
    xmlns:coreContracts="using:XMCL2025.Core.Contracts.Services"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewModels:下载ViewModel />
    </Page.DataContext>

    <Grid x:Name="ContentArea" Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 最新版本卡片 -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- 最新正式版 -->
            <Border CornerRadius="8" Padding="15" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" BorderThickness="1" Background="{ThemeResource CardBackgroundFillColorDefault}">
                <StackPanel>
                    <TextBlock Text="最新正式版" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondary}" />
                    <TextBlock Text="{x:Bind ViewModel.LatestReleaseVersion, Mode=OneWay}" Style="{StaticResource TitleTextBlockStyle}" Foreground="{ThemeResource TextFillColorPrimary}" />
                    <Button Content="下载" Style="{StaticResource DefaultButtonStyle}" HorizontalAlignment="Left" Margin="0,10,0,0"
                            Command="{x:Bind ViewModel.DownloadVersionCommand}" CommandParameter="{x:Bind ViewModel.LatestReleaseVersion, Mode=OneWay}" />
                </StackPanel>
            </Border>

            <!-- 最新快照版 -->
            <Border Grid.Column="2" CornerRadius="8" Padding="15" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" BorderThickness="1" Background="{ThemeResource CardBackgroundFillColorDefault}">
                <StackPanel>
                    <TextBlock Text="最新快照版" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondary}" />
                    <TextBlock Text="{x:Bind ViewModel.LatestSnapshotVersion, Mode=OneWay}" Style="{StaticResource TitleTextBlockStyle}" Foreground="{ThemeResource TextFillColorPrimary}" />
                    <Button Content="下载" Style="{StaticResource DefaultButtonStyle}" HorizontalAlignment="Left" Margin="0,10,0,0"
                            Command="{x:Bind ViewModel.DownloadVersionCommand}" CommandParameter="{x:Bind ViewModel.LatestSnapshotVersion, Mode=OneWay}" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- 搜索和刷新 -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <AutoSuggestBox 
                x:Name="SearchBox"
                PlaceholderText="搜索版本..."
                Text="{x:Bind ViewModel.SearchText, Mode=TwoWay}"
                QuerySubmitted="SearchBox_QuerySubmitted"
                HorizontalAlignment="Stretch" />

            <Button 
                Grid.Column="1"
                Margin="10,0,0,0"
                Command="{x:Bind ViewModel.RefreshVersionsCommand}">
                <Grid>
                    <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xe72c;" Visibility="{x:Bind ViewModel.IsRefreshing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}" />
                    <ProgressRing IsActive="{x:Bind ViewModel.IsRefreshing, Mode=OneWay}" Width="20" Height="20" Visibility="{x:Bind ViewModel.IsRefreshing, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                </Grid>
            </Button>
        </Grid>

        <!-- 版本列表 -->
        <Grid Grid.Row="2">
            <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center" />
            <ListView 
                x:Name="VersionsListView"
                ItemsSource="{x:Bind ViewModel.FilteredVersions, Mode=OneWay}"
                IsItemClickEnabled="True"
                ItemClick="VersionsListView_ItemClick"
                SelectionMode="None"
                Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Inverse}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="coreContracts:VersionEntry">
                        <StackPanel Padding="10">
                            <TextBlock Text="{x:Bind Id}" Style="{StaticResource SubtitleTextBlockStyle}" />
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                <TextBlock Text="{x:Bind Type}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="Gray" />
                                <TextBlock Text=" | " Style="{StaticResource CaptionTextBlockStyle}" Foreground="Gray" />
                                <TextBlock Text="{x:Bind ReleaseTime}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="Gray" />
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
